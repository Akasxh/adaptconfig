services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
      - db-data:/app/data
    environment:
      - FINSPARK_DEBUG=true
      - FINSPARK_DATABASE_URL=sqlite+aiosqlite:///./data/adaptconfig.db
      - FINSPARK_SECRET_KEY=docker-dev-key-change-in-production-use-openssl-rand-hex-32
      - FINSPARK_ENCRYPTION_KEY=docker-dev-enc-change-in-production-use-openssl-rand-hex-32
      - FINSPARK_GEMINI_API_KEY=${FINSPARK_GEMINI_API_KEY:-}
      - FINSPARK_GEMINI_MODEL=gemini-3-flash-preview
      - FINSPARK_AI_ENABLED=true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        VITE_API_URL: http://localhost:8000
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  uploads:
  db-data:
